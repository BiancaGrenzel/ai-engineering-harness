# Context Engineering

Tools that help select, retrieve, compress, cache, or otherwise manage the information loaded into a model context window.

Focus is on **what enters context** and how it is structured, not solely on billing tokens (see [`../token/`](../token/) when token reduction is the primary purpose).

No Tools are cataloged in this category yet.
