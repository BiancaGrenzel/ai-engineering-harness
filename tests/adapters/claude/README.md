# Claude adapter test fixtures

Isolated project tree for Claude adapter tests. Do not point tests at a real
developer `.claude/` directory.

Schemas are copied from repository `schemas/` at test time (not stored here).
